<%= form_for(@gold_balance) do |f| %>
  <% if @gold_balance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gold_balance.errors.count, "error") %> prohibited this gold_balance from being saved:</h2>

      <ul>
      <% @gold_balance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :User_id %><br>
    <%= f.text_field :User_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
